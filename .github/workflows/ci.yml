name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: node --check mcp-server/server/index.js
      - run: node --check mcp-server/server/remote-client.js
      # The bridge's deps are installed here, not only in CD: the worker's
      # protocol contract test imports mcp-server/server/remote-client.js, which
      # resolves @modelcontextprotocol/client out of mcp-server/node_modules.
      - name: mcp-server install
        run: npm ci
        working-directory: mcp-server
      - name: mcp-server client migration tests
        run: node --test test/*.test.mjs
        working-directory: mcp-server
      - name: worker install + typecheck + dry-run deploy + unit and contract tests
        run: |
          npm ci
          npm run typecheck
          npx wrangler deploy --dry-run --outdir dist
          npm test
        working-directory: worker

  # Gate job: single Required status check for branch protection.
  # Add new jobs to `needs` when CI grows. No Settings change needed.
  CI:
    if: always()
    needs: [test]
    runs-on: ubuntu-latest
    steps:
      - run: |
          if [[ "${{ needs.test.result }}" != "success" ]]; then
            echo "CI failed: test=${{ needs.test.result }}"
            exit 1
          fi
